/**
 * Hook for managing the teaching session state.
 */

export function useSession() {
  // Placeholder — will manage session lifecycle via API
  return { sessionId: null, status: "idle" as const };
}
