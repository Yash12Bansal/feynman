# TODO(DEADCODE): file unused in active pipelines (precompute-gen / lecture-playback / ask-feynman). See docs/engineering/13-redundant-code-audit.md Group 1. Safe to delete.
# """Experimental, opt-in feature scaffolding.

# Code under `experiments/` is for active R&D — comparing approaches,
# measuring tradeoffs, validating hypotheses. Promote a proven approach
# into the main `feynman.*` tree once it's earned its keep.
# """
