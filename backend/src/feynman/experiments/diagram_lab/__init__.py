# TODO(DEADCODE): file unused in active pipelines (precompute-gen / lecture-playback / ask-feynman). See docs/engineering/13-redundant-code-audit.md Group 1. Safe to delete.
# """Diagram generation testbed — plugin-based strategy comparison.

# Lives at API path `/api/diagtest/*` and frontend route `#/diagram_generation_test`.
# Goal: rapid iteration on math-diagram generation — latency, accuracy, and
# interaction (annotation) — across multiple generation strategies and models.

# Promote winning strategies into `feynman.agent.design_bridge` once proven.
# """
